@page "/login"
@layout Layout.PublicLayout
@inject AuthService AuthService
@inject NavigationManager NavigationManager

<PageTitle>Giriş Yap - BakımSuite</PageTitle>

<div class="auth-shell auth-shell-login">
    <div class="auth-panel auth-panel-brand">
        <div style="display:flex;align-items:center;gap:0.75rem;margin-bottom:1.5rem">
            <div style="width:2.8rem;height:2.8rem;border-radius:14px;background:linear-gradient(135deg,#f97316,#fb923c);display:grid;place-items:center;color:#fff;font-weight:800;font-size:0.95rem;letter-spacing:-0.02em">BS</div>
            <div style="color:#f1f5f9;font-weight:700;font-size:1rem">BakımSuite</div>
        </div>
        <div class="eyebrow" style="color:#fdba74">Servis yönetimi</div>
        <h1 class="auth-title">Günün operasyonunu tek panelden başlatın</h1>
        <p class="auth-copy">
            Servis kabul, randevu, tahsilat ve günlük özet ekranlarına hızlı erişim için giriş yapın.
            BakımSuite, eski masaüstü servis yazılımlarının dağınık menü yapısını modern web deneyimine taşır.
        </p>

        <div class="auth-feature-list">
            <div class="auth-feature-item">Hızlı servis kabul ve iş emri akışı</div>
            <div class="auth-feature-item">Müşteri, araç ve randevu takibi</div>
            <div class="auth-feature-item">Tahsilat ve günlük operasyon özeti</div>
        </div>
    </div>

    <div class="auth-panel auth-panel-form auth-panel-narrow">
        <div class="section-header mb-2">
            <div>
                <div class="section-title">Giriş Yap</div>
                <div class="section-subtitle">Tenant, kullanıcı ve şifre ile devam edin.</div>
            </div>
        </div>

        <EditForm Model="_model" OnValidSubmit="LoginAsync">
            <DataAnnotationsValidator />

            <div class="form-grid auth-form-grid">
                <div class="full-span">
                    <label class="form-label">İşletme Kodu</label>
                    <InputText class="form-control" @bind-Value="_model.TenantIdentifier" placeholder="asya-motor" autocomplete="organization" />
                </div>

                <div class="full-span">
                    <label class="form-label">E-posta</label>
                    <InputText class="form-control" @bind-Value="_model.Email" placeholder="owner@servisiniz.com" autocomplete="email" />
                </div>

                <div class="full-span">
                    <label class="form-label">Şifre</label>
                    <InputText class="form-control" type="password" @bind-Value="_model.Password" placeholder="••••••••" autocomplete="current-password" />
                </div>
            </div>

            @if (!string.IsNullOrWhiteSpace(_error))
            {
                <div class="alert alert-danger mt-3">@_error</div>
            }

            <div class="auth-actions">
                <button type="submit" class="btn btn-primary btn-lg" disabled="@_isSubmitting">
                    @(_isSubmitting ? "Giriş yapılıyor..." : "Giriş Yap")
                </button>
                <a class="auth-link" href="/register">İlk servisini oluşturmak için kayıt ol →</a>
            </div>
        </EditForm>
    </div>
</div>

@code {
    private readonly LoginRequest _model = new();
    private bool _isSubmitting;
    private string? _error;

    private async Task LoginAsync()
    {
        _isSubmitting = true;
        _error = null;

        try
        {
            await AuthService.LoginAsync(_model);
            NavigationManager.NavigateTo("/dashboard");
        }
        catch (Exception ex)
        {
            _error = ex.Message;
        }
        finally
        {
            _isSubmitting = false;
        }
    }
}
