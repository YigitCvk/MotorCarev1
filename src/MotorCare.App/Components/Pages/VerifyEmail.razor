@page "/verify-email"
@layout Layout.PublicLayout
@using Microsoft.AspNetCore.WebUtilities
@inject AuthService AuthService
@inject NavigationManager NavigationManager

<PageTitle>E-posta Dogrulama - BakimSuite</PageTitle>

<div class="auth-shell auth-shell-login">
    <div class="auth-panel auth-panel-brand">
        <div class="eyebrow" style="color:#fdba74">Hesap aktivasyonu</div>
        <h1 class="auth-title">E-posta dogrulama</h1>
        <p class="auth-copy">Dogrulama baglantisi gecerliyse hesabinizi aktif hale getiririz.</p>
    </div>

    <div class="auth-panel auth-panel-form auth-panel-narrow">
        <div class="section-header mb-2">
            <div>
                <div class="section-title">Dogrulama Durumu</div>
                <div class="section-subtitle">Sonuc burada gorunur.</div>
            </div>
        </div>

        @if (_isLoading)
        {
            <div class="state-loading">Dogrulama yapiliyor...</div>
        }
        else if (!string.IsNullOrWhiteSpace(_error))
        {
            <div class="alert alert-danger">@_error</div>
            <div class="auth-actions">
                <a class="auth-link" href="/login">Giris ekranina don</a>
            </div>
        }
        else
        {
            <div class="alert alert-success">@_message</div>
            <div class="auth-actions">
                <a class="btn btn-primary btn-lg" href="/login">Giris Yap</a>
            </div>
        }
    </div>
</div>

@code {
    private bool _isLoading = true;
    private string? _message;
    private string? _error;

    [SupplyParameterFromQuery] public string? Email { get; set; }
    [SupplyParameterFromQuery] public string? Token { get; set; }

    protected override async Task OnParametersSetAsync()
    {
        if (!_isLoading)
        {
            return;
        }

        if (string.IsNullOrWhiteSpace(Email) || string.IsNullOrWhiteSpace(Token))
        {
            _error = "Gecersiz dogrulama baglantisi.";
            _isLoading = false;
            return;
        }

        try
        {
            var response = await AuthService.VerifyEmailAsync(new VerifyEmailRequest
            {
                Email = Email,
                Token = Token
            });

            _message = response?.Message ?? "E-posta adresiniz dogrulandi.";
            var uri = QueryHelpers.AddQueryString("/login", new Dictionary<string, string?>
            {
                ["email"] = Email,
                ["message"] = _message
            });
            NavigationManager.NavigateTo(uri);
        }
        catch (Exception ex)
        {
            _error = ex.Message;
        }
        finally
        {
            _isLoading = false;
        }
    }
}
